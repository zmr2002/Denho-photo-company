{
  "name": "tianho-photo-company",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @tianho/web",
    "build": "npm run build --workspace @tianho/web",
    "start": "npm run start --workspace @tianho/web",
    "lint": "npm run lint --workspace @tianho/web",
    "prisma:generate": "npm run prisma:generate --workspace @tianho/web",
    "prisma:migrate": "npm run prisma:migrate --workspace @tianho/web",
    "prisma:seed": "npm run prisma:seed --workspace @tianho/web",
    "prisma:studio": "npm run prisma:studio --workspace @tianho/web"
  }
}
